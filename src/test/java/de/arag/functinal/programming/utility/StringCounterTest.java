package de.arag.functinal.programming.utility;

class StringCounterTest {

    /*private StringCounter stringCounter = StringCounter.getGetInstance();
    @Test
    void countString() {
        //Given
        String input = "\nThis is just for test purpose .\nWe gonna count the number of the all words .\nIn the case that the algorithm works correctly .\nPlease display the works with the corresponding counter .\nThe case sensitive will also be tested.";
        //String input = "Hallo";

        //When
        Map<String, Long> map = stringCounter.countString(input, "\\s+");

        //Then
        assertThat(map).isNotNull();
    }*/
}